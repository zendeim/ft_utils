{
  "version": "2.0.0",
  "tasks": [
    {
		"label": "build",
		"type": "shell",
		"command": "bear",
		"options": {"cwd": "${workspaceFolder}"},
		"args": ["--output", "build/compile_commands.json", "--","make", "asan"],
		"group": { "kind": "build", "isDefault": true },
    }
  ]
}